# Core scientific stack
numpy>=1.23
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
joblib>=1.3

# Visualization & dashboard
streamlit>=1.30
plotly>=5.15
matplotlib>=3.7
seaborn>=0.12

# Deep learning (optional; LSTM falls back to a sklearn surrogate if absent)
tensorflow>=2.13 ; platform_system != "Darwin" or platform_machine != "arm64"
tensorflow-macos>=2.13 ; platform_system == "Darwin" and platform_machine == "arm64"

# Network / packet capture (used in live mode only)
scapy>=2.5

# Configuration & utilities
PyYAML>=6.0
python-dateutil>=2.8
rich>=13.0
tqdm>=4.65

# Testing
pytest>=7.4
pytest-cov>=4.1
